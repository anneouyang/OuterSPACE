import torch

from sparse_util import *
from util import *

# a = torch.rand((1000, 1000))
# print(get_sparsity(a))
# a = prune_to_sparsity(a, 0.001)
# print(get_sparsity(a))

# save_tensor_as_mtx(a, "tmp.mtx")
